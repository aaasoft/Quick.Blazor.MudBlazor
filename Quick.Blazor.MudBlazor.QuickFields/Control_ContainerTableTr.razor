@using Quick.Fields

<tr class="mud-table-row @ControlUtils.GetCommonClass(Field)" style="@Field.Html_Style">
    @if (Field.Children != null)
    {
        @foreach (var child in Field.Children)
        {
            <Control Field="@child" />
        }
    }
</tr>

@code
{
    [Parameter]
    public FieldForGet Field { get; set; }
}