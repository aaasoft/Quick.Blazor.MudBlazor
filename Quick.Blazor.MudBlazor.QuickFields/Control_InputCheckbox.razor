@using MudBlazor
@using Quick.Fields
@using global::MudBlazor

<InputBase Field="@Field">
    <MudSwitch @bind-Value="Value" Label="@Field.Name" Size="ControlUtils.GetSize(Field)"
               Color="ControlUtils.GetColor(Field)"
               Required="@(Field.Input_AllowBlank==null?false:Field.Input_AllowBlank.Value)"
               ReadOnly="@ControlUtils.GetInputReadOnly(Field)" />
</InputBase>

@code
{
    [Parameter]
    public FieldForGet Field { get; set; }

    private bool Value
    {
        get
        {
            if (bool.TryParse(Field.Value, out var ret))
                return ret;
            return false;
        }
        set
        {
            Field.Value = value.ToString();
        }
    }
}